# Invalid fixture cases

The unit tests derive bounded invalid cases from valid text fixtures. Covered failures include missing fields, traversal paths, malformed checksums, unknown provenance, unresolved references, non-approved reviews, and mismatched export metadata. No binary fixture is required.
